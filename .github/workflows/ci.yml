name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true

      # The `app` package embeds frontend/dist, which only exists after
      # `wails build`; on a clean checkout `go list ./...` itself fails on it.
      # app is vetted, tested, and built by the dedicated app.yml workflow, so
      # this CLI-focused job targets the cmd/ and internal/ packages directly.
      - name: go vet
        run: go vet ./cmd/... ./internal/...

      - name: go test
        run: go test -race -cover ./cmd/... ./internal/...

      - name: go build
        # Confirms the binary compiles end-to-end on every commit.
        run: go build -o /tmp/t4p ./cmd/t4p

      - name: Smoke-test non-interactive init
        # If the wizard's non-interactive path ever regresses, this catches
        # it before any release tag is cut.
        run: |
          /tmp/t4p version
          /tmp/t4p init --use-case=coding-agent --priority=quality
